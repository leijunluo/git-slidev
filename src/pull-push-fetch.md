# fetch

```bash
$ git fetch
```

# pull

```bash
$ git pull
$ git pull <remote>
$ git fetch && git merge
```

# push

```bash
$ git push -u origin HEAD
$ git push

$ git push origin master
$ git push origin HEAD
```